define(function (require) {
    'use strict';

    var Model = require('backbone').Model;

    /**
     * @class    Job
     * @extends {Backbone.Model}
     */
    var Job = Model.extend({});

    return Job;
});